---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "f5distributedcloud_virtual_host Resource - f5distributedcloud"
subcategory: ""
description: |-
  The f5distributedcloud_virtual_host resource manages Virtual Hosts in F5 Distributed Cloud.
  Virtual Hosts define HTTP routing configurations for domain-based traffic.
  Example Usage
  
  resource "f5distributedcloud_virtual_host" "example" {
    name        = "my-vhost"
    namespace   = "system"
    description = "Virtual host for web application"
    domains     = ["app.example.com", "www.example.com"]
  }
---

# f5distributedcloud_virtual_host (Resource)

The `f5distributedcloud_virtual_host` resource manages Virtual Hosts in F5 Distributed Cloud.

Virtual Hosts define HTTP routing configurations for domain-based traffic.

## Example Usage

```hcl
resource "f5distributedcloud_virtual_host" "example" {
  name        = "my-vhost"
  namespace   = "system"
  description = "Virtual host for web application"
  domains     = ["app.example.com", "www.example.com"]
}
```



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String)
- `namespace` (String)

### Optional

- `description` (String)
- `domains` (List of String) List of domains for this virtual host.

### Read-Only

- `id` (String) The ID of this resource.
